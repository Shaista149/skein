{
  "name": "skein",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.2.0",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "three": "0.128.0"
  }
}